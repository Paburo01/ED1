#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <cstdlib>
#define maxlist 5
using namespace std;
struct TNodo {
    char dato[4];
    TNodo *sig;
    };

void numeranodos (TNodo **elementos);
int main()
{
    TNodo *elementos;


    numeranodos (&elementos);
    return 0;
}


void numeranodos (TNodo **elementos){
    TNodo *p=*elementos;
    TNodo *nuevo;
    TNodo *aux;
    int i=1;
    char cad[10];
    while(p!=NULL){
        nuevo=new TNodo;
        itoa(i,cad,10);
        strcpy(nuevo->dato,cad);
            if(p==*elementos){
                *elementos=nuevo;
                nuevo->sig=p;
                aux=p;
            }else{
                aux->sig=nuevo;
                nuevo->sig=p;
                aux=p;
            }
            p=p->sig;
            i++;
        }


};

